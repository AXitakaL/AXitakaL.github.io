<div class="gallery">
	{% for img in gallery1 %}
		
			<article class="from-left">
				<a href="{{img.url}}" class="image fit"><img src="{{img.url}}" "{{img.title}}" alt="" /></a>
			</article>
		
			<article class="from-right">
				<a href="{{img.url}}" class="image fit"><img src="{{img.url}}" "{{img.title}}" alt="" /></a>
			</article>
		
	{% endfor %}
</div>